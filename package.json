{
  "name": "dalili-weather-alert",
  "private": true,
  "version": "1.0.0",
  "description": "Dalili — hyperlocal weather alert system for Kenya (BAC-2202).",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 4173",
    "lint": "echo \"(add eslint here if desired)\" && exit 0",
    "start": "node server/index.js",
    "render-build": "npm install && npm run build && npm --prefix server install"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "lucide-react": "0.383.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "recharts": "^2.12.7"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.3.4"
  }
}
