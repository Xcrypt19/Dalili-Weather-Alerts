# Browser Google Maps key (Maps JavaScript API) — FR-11, FR-42.
# Restrict this key to your domains in Google Cloud Console.
VITE_GOOGLE_MAPS_API_KEY=

# Optional: override API base (defaults to same origin /api via the dev proxy).
VITE_API_BASE=/api
